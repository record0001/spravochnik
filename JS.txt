
	length		-	Длина что-то.length

	slice()		-	срез строки что-то.slice(начало) или что-то.slice(начало, конец) что-то.slice(номер символа)

	toUpperCase()	-	перевод строки в верхний регистр

	toLowerCase()	-	перевод строки в нижний регистр

	массив[]	-	массив

	push()		-	добавление в конец массива массив.push(что-то)

	unshift()	-	добавление в начало массива массив.unshift(что-то)

	pop()		-	удаляет последний элемент массива и возвращает его значение // переменная = массив.pop(что-то)

	shift()		-	удаляет первый элемент массива и возвращает его значение // переменная = массив.shift(что-то)

	concat()	-	сливает массивы первый_массив.concat(второй_массив, если надо, то еще массивы)
				все из первого будет в начале, далее значения второго, сами массивы не изменяются

	indexOf()	-	возвращает индекс (первого найденного)элемента массива массив.indexOf("значение"), получаем индекс,
				если значение не найдено, то indexOf вернет "-1"

	join()		-	переводит массив в строку, по умолчанию join() через запятую массив.join() или массив.join("разделитель")

	Math.random()	-	генерирует число от 0 до 1 Math.random() или Math.random()*1000

	Math.floor()	-	округляет до целого, по факту отбрасывает все после запятой Math.floor(5.555) вернет 5
				Math.floor(Math.random()*1000) случайное целое

	объект{}	-	объект,		доступ объект.ключ или объект["ключ"]
					добавление  объект.ключ="значение" или объект["ключ"]="значение"
						объект["ключ"]={ключ_1:"значение 1", ключ_2:"значение 2"}
					
	alert()		-	всплывающее окно

	prompt()	-	всплывающее диалоговое окно

	confirm()	-	всплывающее диалоговое окно выдает true или false, возможность обработать if else

	break		-	выводит из цикла

	setTimeout()	-	Выполнение через время setTimeout(что-то, млсек)

	clearTimeout()	-	Отмена setTimeout() clearTimeout(переменная в которую занесен setTimeout, который вызывается через переменную)

	setInterval()	-	Повторение кода через время setInterval(что-то, млсек)

	clearInterval()	-	Отмена SetInterval()

offset()	-	??? Перемещает объект что-то.offset(left или top: px)
				В отличие от e.pageX Выдает координаты не от начала страницы,
					а от начала объекта на котором кликнули

e.offsetX	-	Получить координату X

e.offsetY	-	Получить координату Y

prototype	-	Добавляет объектам общие методы объект.prototype.метод = function () {}

getContext()	-	Получить контекст для рисования e.getContext("2d") 2d - Двухмерное

fillRect()	-	Прямоугольник(начало X, начало Y, длина X, длина Y) e.getContext("2d").fillRect(100,100,25,25)

clearRect()	-	Очистка области прямоугольник(начало X, начало Y, длина X, длина Y) ctx.clearRect(0,5,350,350);

fillStyle	-	Указываем цвет рисования заливка fillStyle = "цвет"

strokeRect()	-	Контур прямоугольника e.strokeRect(35,250,35,35)

strokeStyle	-	Цвет контура strokeStyle = "цвет"

lineWidth	-	Толщина контура lineWidth = 1... и т.д.

beginPath()	-	Будем рисовать линию

moveTo()	-	Начальные координаты линии e.moveTo(100,300); если несколько подряд, то пишем вначале и все далее lineTo()

lineTo()	-	Конечные координаты линии lineTo(235,290);

stroke()	-	Отрисовываем линию stroke();

fill()		-	Заливает отрисованную линиями замкнутую область e.fill()

arc()		-	Будем рисовать окружность ctx.arc(180, 140, 100, 0, Math.PI*2, false);
				ctx.arc(x, y, radius, начальная точка, конечная точка, направление)

fillText()	-	Рисование текста e.fillText("Текст", x, y)

textBaseline	-	Выравнивание текста по базовой линии bottom middle top e.textBaseline = "top" относительно самого текста

textAlign	-	Выравнивание текста по горизонтали left, right, center e.textAlign = "center" относительно самого текста

font		-	Напмсание текста e.font = "20px arial"







			--------------------------------------

			https://learn.javascript.ru/metrics

e.clintWidth	-	Ширина элемента без рамки

e.clientHeight	-	Высота элемента без рамки


e.offsetWidth	-	Ширина элемента с рамкой

e.offsetHeight	-	Высота элемента с рамкой


e.getBoundingClientRect()	-	возвращает объект, содержащий координаты элемента (top, left, bottom, right)

			--------------------------------------

e.startsWith()	-	e.startsWith("часть строки с которой начинается e")

e.endstWith()	-	e.endsWith("часть строки чем заканчивается e")

e.includes()	-	e.includes(часть строки которую ищем в e)

e.repeat()	-	Дублирует строку e несколько раз e.repeat(3)

e.trim()	-	Удаляет пробелы в начале и еонце строки

e.trimStart()	-	Удаляет пробелы в начале строки

e.trimEnd()	-	Удаляет пробелы в конце строки

e.padStart()	-	Минимальная длина строки e.padStart(количество,"чтодобавить"), добавит в начало недостающие символы
				по умолчанию пробелы

e.padEnd()	-	Минимальная длина строки e.padEnd(количество,"чтодобавить"), добавит в конец недостающие символы
				по умолчанию пробелы

e.reduce()	-	массив.reduce()




HTML DOM События

https://html5css.ru/jsref/dom_obj_event.php








https://developer.mozilla.org/ru/docs/web/javascript




























https://www.youtube.com/playlist?list=PLcvhF2Wqh7DNVy1OCUpG3i5lyxyBWhGZ8







